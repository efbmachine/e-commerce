Not started:
    -Email on order (and on admin page)

    e-categorisation or order(chronolical, address:quartier, bigOrder(>amount || >articleNumber), )
    -main admin:
        -let main admin CRUD other admin

    -admin side:
        section utilisateur
            (nombres, commandes(nombres), addresse, numero, email)
    ?-automate (email sent after 5 days if they didn't order yet)
    -fix bug at order when it creates 2 instead of 1
    -put gabonese time instead of current
    - put categories by alph order
    - payment method:
        - airtel money / mobiCash
        - creditCARD (paypal && regural cc)

    - quick add to cart
    - put pictures to subCategories and categories
    - reduce product's square size
    - add footers:
        -join us
            - partner stores
        -help
            - faq
            - contact us
    - center quantity
    - make qty input center
    - put ogooue on one side of logo and delivery on the other (in orange)
    - automatisasion du choix de fournisseur par region et detention du produit
    - try to remove product's image background so it is transporant
    - change from session to cookies
    - display cart on the side after a product is added
    - login modal when doing something that requires login
    - implement promo by price (and display purcentage difference)
    -
____________________________________________________________________________________________________________________
In process:
    -tags(left to do:
        -put tags on client index
        -manage tags
        -implement search by products . tags only (add product's name to product's tags)

        )

    -add provider on product
    -improve user profile (edit admin order accordingly)




____________________________________________________________________________________________________________________
Completed:
    -(whats app) phone number required
    -Description on top of box instead of side
    -create tags on Submit
    -get tags from tags table
    -put autocomplete on editProduct
    -check if edit images work
    -on order admin side:
        display name
        display address
        can pick traitement, preparation, livraison, livree
    -main admin:
        -admin login
        -admin user
    -rayon nav bar at the bottom of main nav mar (in black)
    -multiple delete on product
    -display of addresses
    - implement login by facebook
